export type IMeta = {
    limit: number;
    page: number;
    total: number;
}
export type FormValues = {
    id: string;
    password: string
}
export type IDepartment = {
    id: string,
    title: string,
    createdAt: string,
    updatedAt: string,
    _id: string
}