export enum tagTypes {
    user = "user",
    depatment = 'depatment',
    faculty = 'faculty',
    admin = 'admin'
}

export const tagTypesList = [tagTypes.user, tagTypes.faculty, tagTypes.depatment]